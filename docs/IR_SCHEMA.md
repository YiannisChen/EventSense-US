## IR Schema Notes

Canonical JSON Schemas live in:

- `packages/schemas/event_ir.schema.json`
- `packages/schemas/report_ir.schema.json`

These define the contract for IR snapshots used by the agentic pipeline and the UI.


